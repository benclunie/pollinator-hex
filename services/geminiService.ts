import { EcologicalReport } from '../types';

// Service neutralized to allow deployment without API keys or external dependencies
export const generateEcologicalReport = async (): Promise<EcologicalReport | null> => {
  return null;
};